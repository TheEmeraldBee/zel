const hello = fn(name) {
	print("Hello ", name);
};

const user = read_line();
hello(user);

var x = 5;
if user == "Brighton" {
	print("Admin user logged in");
	var x = 4;
};

{
	var x = 10;
	print("x in scope: ", x);
}

print(x);
