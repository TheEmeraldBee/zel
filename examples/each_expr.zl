/*
This is a basic block comment that should be completely ignored
*/

// This is a line comment that should also be ignored

let int64 = int(64)

let double = fn(a: int64) -> int64 {
  a * 2
}

let main = fn() {
  let my_val = struct {a: string, b: int(64) } [ a: "hello, world", b: 20 ];
  debug(my_val.a)
}
